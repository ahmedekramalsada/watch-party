# 🎬 دليل الاستخدام السريع - Watch Party

## 🚀 التشغيل في 3 خطوات

### 1️⃣ حط الفيلم

```bash
# انسخ الفيلم بتاعك
cp /path/to/movie.mp4 media/movie.mp4

# أو استخدم السكريبت الجاهز
./upload-video.sh /path/to/video.mkv
```

### 2️⃣ شغّل السيرفر

```bash
# الطريقة السهلة
./start.sh

# أو يدوياً
docker-compose up -d
```

### 3️⃣ افتح في المتصفح

```
http://localhost
```

من موبايلك أو أي جهاز تاني على نفس الشبكة:
```
http://عنوان_السيرفر_بتاعك
```

---

## 💡 كيف تستخدمه؟

### عشان تعمل Watch Party جديد:

1. افتح `http://localhost`
2. اكتب اسمك
3. اكتب اسم للغرفة (أو استخدم الاسم المولّد تلقائياً)
4. اضغط "إنشاء غرفة جديدة"
5. شارك الرابط مع أصحابك

### عشان تنضم لـ Watch Party:

1. خد الرابط من صاحبك
2. أو اكتب كود الغرفة يدوياً
3. اضغط "انضمام للغرفة"
4. استمتع بالمشاهدة المتزامنة!

---

## 🎮 المميزات

- ✅ Play/Pause/Seek متزامن تماماً
- ✅ شات مباشر مع أصحابك
- ✅ شغال على الموبايل والكمبيوتر
- ✅ كل غرفة ليها كود خاص
- ✅ Low latency - تأخير أقل من 5 ثواني

---

## 🔧 حل المشاكل

### الفيديو مش شغال؟

```bash
# تأكد إن الفيلم موجود
ls -lh media/

# شوف اللوجات
docker-compose logs srs
```

### مش قادر تتصل بالغرفة؟

```bash
# أعد تشغيل الـ WebSocket
docker-compose restart websocket
```

### الـ Sync مش شغال؟

- رفرش الصفحة
- تأكد إن كلكم في نفس الغرفة
- شوف الـ Console في المتصفح (F12)

---

## 🌐 استخدام خارج الشبكة المحلية

### باستخدام ngrok (الأسرع):

```bash
ngrok http 80
```

شارك رابط ngrok مع أصحابك.

### باستخدام سيرفر (VPS):

1. ارفع المشروع على DigitalOcean/AWS
2. وجّه الدومين للسيرفر
3. فعّل SSL بـ Let's Encrypt

---

## 🛑 إيقاف السيرفر

```bash
docker-compose down
```

---

## 📱 معلومات إضافية

### البورتات المستخدمة:
- 80 → NGINX (الواجهة الرئيسية)
- 1935 → RTMP (استقبال الفيديو)
- 8080 → SRS HTTP API
- 3000 → WebSocket (مخفي وراء NGINX)

### متطلبات الـ Video:
- الفورمات: MP4, MKV, AVI, MOV
- يُفضل: H.264 video + AAC audio
- الاسم لازم يكون: `movie.mp4`

---

## 💻 الأوامر المفيدة

```bash
# شوف اللوجات Live
docker-compose logs -f

# شوف لوج سيرفر معين
docker-compose logs websocket

# أعد تشغيل سيرفر معين
docker-compose restart srs

# إيقاف وحذف كل حاجة
docker-compose down -v
```

---

## 🎨 تخصيص الألوان

افتح `frontend/style.css` وغيّر:

```css
:root {
    --primary: #6366f1;     /* اللون الأساسي */
    --secondary: #8b5cf6;   /* اللون الثانوي */
    --bg: #0f172a;          /* لون الخلفية */
}
```

---

## 📞 الدعم

لو عندك مشكلة:

1. شوف قسم "حل المشاكل" فوق
2. راجع اللوجات: `docker-compose logs`
3. تأكد إن كل البورتات فاضية

---

## 🎉 بالتوفيق!

استمتع بمشاهدة الأفلام مع أصحابك! 🍿

**ملحوظة:** لو عايز تضيف أكتر من فيديو أو features تانية، شوف `README.md` الكامل.
